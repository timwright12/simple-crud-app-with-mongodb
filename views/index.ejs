<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="UTF-8" />
    <title>Star Wars Quote App</title>

    <style>
      h1 {
        border-bottom: 1px solid #999;
        padding: 0 0 .5rem;
      }

      h2, form {
        margin: 0 0 1rem;
      }

      form {
        margin-bottom: 2rem;
      }

      form div {
        margin: 0 0 .5rem;
      }
      .col {
        box-sizing: border-box;
        padding: 0 1rem 2rem;
      }
      @media screen and ( min-width: 700px ) {
        .col {
          width: 33.333333%;
          float: left;
        }
      }

    </style>

  </head>
  <body>
    <h1>Quote Manager</h1>
    
    <div class="col">

      <h2> Add new quote </h2>

      <form action="/quotes" method="post">
        <div>
          <label for="name">Name:</label>
          <input type="text" placeholder="name" name="name" id="name" />
        </div>
        <div>
          <label for="quote">Quote:</label>
          <input type="text" placeholder="quote" name="quote" id="quote" />
        </div>
        <button type="submit">Submit</button>
      </form>

      <h2> Quotes </h2>

      <ul>
        <!-- Loop through quotes -->
        <% for(var i = 0; i < quotes.length; i++) {%>
          <li>
            <!-- Output name from the iterated quote object -->
            <span><%= quotes[i].name %></span>:
            <!-- Output quote from the iterated quote object -->
            <span><%= quotes[i].quote %></span>
          </li>
        <% } %>
      </ul>
    </div>

    <div class="col">
      <h2>Darth Vader invades!</h2>
      <p>Replace first Yoda's quote with a quote written by Darth Vader</p>
      <button type="button" id="update-button">Replace Yoda's quote</button>
    </div>

    <div class="col">
      <h2>Remove Darth Vader!</h2>
      <p>Delete one Darth Vader's quote. Does nothing if there are no more Darth Vader's quote
      </p>
      <button id="delete-button">Delete Darth Vader's quote</button>
      <div id="message" aria-atomic="true" aria-live="polite"></div>
    </div>

    <script src="/main.js"></script>

  </body>
</html>